#ifndef SETTINGS_HPP
#define SETTINGS_HPP

#include <variant>
#include <string>
#include <cstdint>
#include <string_view>
#include <vector>

namespace proj
{
    void init(const std::vector<std::string> &args);
}
#endif /* SETTINGS_HPP */
